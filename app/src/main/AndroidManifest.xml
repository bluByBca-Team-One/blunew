<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- HAPUS baris: package="com.example.bluapp" -->

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BcaBlu1">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".fragment_home" />
        <activity android:name=".SavingActivity" />
        <activity android:name=".BluinvestActivity" />
        <activity android:name=".InsuranceActivity" />
        <activity android:name=".KeuanganActivity" />
        <activity android:name=".TransaksiActivity" />
        <activity android:name=".MetodePembayaranActivity" />
        <activity android:name=".RiwayatActivity" />
        <activity android:name=".ProfilActivity" />
        <activity android:name=".SecurityActivity" />

    </application>

</manifest>
